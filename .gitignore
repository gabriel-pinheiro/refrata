# Local working notes for agents and humans; never authoritative
research/

node_modules/
dist/
build/
# Desktop's icon and packaging resources, which electron-builder reads.
!refrata-desktop/build/
# Desktop's packages, which electron-builder writes.
refrata-desktop/release/
out/
coverage/
*.log
.DS_Store
.env
.env.*
*.autosave
